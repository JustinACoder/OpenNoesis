# AI Agent Instructions

Use the repository's GitHub Copilot instruction files as the source of truth:

- `.github/copilot-instructions.md` for repository-wide guidance
- `.github/instructions/*.instructions.md` for path-specific guidance

When these files conflict, prefer the most specific file for the files being edited.
